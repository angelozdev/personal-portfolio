---
import HeroView from "./components/hero-view.astro";
import getHeroData from "./get-hero-data";
import { defaultLocale } from "../../../i18n/utils";

const heroData = getHeroData(Astro.currentLocale ?? defaultLocale);
---

<HeroView data={heroData} />
