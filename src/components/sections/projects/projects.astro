---
import { defaultLocale } from "../../../i18n/utils";
import ProjectsView from "./components/projects-view.astro";
import getProjectsData from "./get-projects-data";

const projectsData = getProjectsData(Astro.currentLocale ?? defaultLocale);
---

<ProjectsView data={projectsData} />
