<div class="container">
  <slot />
</div>

<style>
  .container {
    max-width: 1080px;
    width: 100%;
    padding: var(--space-0) var(--space-4);
    margin: var(--space-0) auto;
    box-sizing: border-box;
  }
</style>
