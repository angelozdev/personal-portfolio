import styled from "@emotion/styled";

export const Container = styled.main`
  padding-top: max(69px, calc(4vh + 21px));
  position: relative;
  z-index: 0;
`;

export default { Container };
